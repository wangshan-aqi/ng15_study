import { PipereversePipe } from './pipereverse.pipe';

describe('PipereversePipe', () => {
  it('create an instance', () => {
    const pipe = new PipereversePipe();
    expect(pipe).toBeTruthy();
  });
});
